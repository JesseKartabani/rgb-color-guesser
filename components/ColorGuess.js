import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const ColorGuess = () => {
  return (
    <SafeAreaView>
      <Text>ColorGuess</Text>
    </SafeAreaView>
  );
};

export default ColorGuess;

const styles = StyleSheet.create({});
